export const selectNewTodo = (state) => state.app.newTodo;

export const selectOrder = (state) => state.app.order;

export const selectSearchItem = (state) => state.app.searchItem;

export const selectSortStatus = (state) => state.app.isSorted;
